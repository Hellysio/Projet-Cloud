
<source>
  @type syslog
  port 5140
  bind 0.0.0.0
  tag system
  format json
  path /fluentd/log/containers/*.log
  pos_file /fluentd/log/fluentd.pos
</source>

<match system.**>
  @type file
  path /output/syslog_logs/
  append true
  flush_interval 5s
</match>
