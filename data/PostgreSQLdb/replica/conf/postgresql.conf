listen_addresses = '*' 
port = 5434           

primary_conninfo = 'host=master_db port=5433 user=myuser password=postgres'
hot_standby = on
primary_slot_name = 'replica_slot'
wal_receiver_timeout = 60s

log_connections = on
log_disconnections = on